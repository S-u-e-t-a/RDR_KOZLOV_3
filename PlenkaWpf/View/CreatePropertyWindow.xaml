<Window x:Class="PlenkaWpf.View.CreatePropertyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm1="clr-namespace:PlenkaWpf.VM"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="CreatePropertyWindow" Height="200" Width="300"
        d:DataContext="{d:DesignInstance vm1:CreatePropertyVM}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <StackPanel>
            <hc:TextBox Text="{Binding Property.PropertyName}" hc:InfoElement.Title="Название свойства" />
            <hc:ComboBox hc:InfoElement.Title="Единица измерения" DisplayMemberPath="UnitName"
                         ItemsSource="{Binding Units, Mode=OneWay}" SelectedItem="{Binding Property.Unit, Mode=TwoWay}" />

        </StackPanel>
        <Grid Row="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="AddButton" Width="100" Margin="5" Content="Добавить" />
                <Button x:Name="ExitButton" Width="100" Margin="5" Content="Отмена" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>