<Window x:Class="PlenkaWpf.View.CreatePropertyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlenkaWpf.View"
        xmlns:vm="clr-namespace:ViewModels;assembly=ViewModels"
        mc:Ignorable="d"
        Title="CreatePropertyWindow" Height="200" Width="300"
        d:DataContext="{d:DesignInstance vm:CreatePropertyVM}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <StackPanel>
            <Label Content="Название свойства"/>
            <TextBox></TextBox>
            <Label Content="Единица измерения"/>
            <ComboBox DisplayMemberPath="UnitName"
                      ItemsSource="{Binding Units, Mode=OneWay}" SelectedItem="{Binding Property.Unit, Mode=TwoWay}"></ComboBox>
        </StackPanel>
        <Grid Row="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="AddButton" Width="100" Margin="5" Content="Добавить"/>
                <Button x:Name="ExitButton" Width="100" Margin="5" Content="Отмена" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
